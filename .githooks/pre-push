#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ] && [ "$BYPASS_MAIN_PUSH" != "1" ]; then
  echo "[BLOCKED] Direct push to main is disabled by local policy."
  echo "Use develop + PR/merge flow, or set BYPASS_MAIN_PUSH=1 for emergency." 
  exit 1
fi

exit 0
